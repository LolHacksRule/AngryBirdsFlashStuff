package §&[§
{
   import §9!H§.§7k§;
   import §?A§.LevelMain;
   
   public class §-o§ extends §7k§
   {
      
      public static const LEVEL_END_TIMEOUT_AFTER_LAST_PIG_OR_BIRD_EASTER_EGG_HACK:Number = 10000;
      
      {
         var LEVEL_END_TIMEOUT_AFTER_LAST_PIG_OR_BIRD_EASTER_EGG_HACK:Boolean = true;
         var _loc2_:Boolean = false;
         if(LEVEL_END_TIMEOUT_AFTER_LAST_PIG_OR_BIRD_EASTER_EGG_HACK)
         {
            do
            {
               LEVEL_END_TIMEOUT_AFTER_LAST_PIG_OR_BIRD_EASTER_EGG_HACK = 10000;
            }
            while(!(LEVEL_END_TIMEOUT_AFTER_LAST_PIG_OR_BIRD_EASTER_EGG_HACK || §-o§));
            
         }
      }
      
      public function §-o§(levelMain:LevelMain)
      {
         var _loc2_:Boolean = false;
         var _loc3_:Boolean = true;
         if(!_loc3_)
         {
         }
         if(_loc3_)
         {
            loop0:
            while(true)
            {
               do
               {
                  super(levelMain);
                  continue loop0;
               }
               while(!(_loc3_ || _loc3_));
               
            }
         }
      }
   }
}
